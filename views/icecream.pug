extends layout

block content
  h1= title

    - var items=results
  div(class="container-fluid")
  div(class="row")
   each g in items
    div(class="col-sm-2" ) #{g.FLAVOUR}, #{g.SERVING_SIZE}, #{g.CALORIES_PER_SERVING}